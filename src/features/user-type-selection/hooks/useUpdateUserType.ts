import { useMutation, useQueryClient } from '@tanstack/react-query';

export default function (
  userId: string,
  userType: string,
  accessToken: string,
  refreshToken: string
) {
  // TODO: implement
}
